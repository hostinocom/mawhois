---
// Reusable section wrapper component
const { backgroundColor = 'bg-white' } = Astro.props;
---

<section class={`${backgroundColor} px-6 py-10 md:pb-30 md:px-16 lg:px-24`}>
	<div class="max-w-7xl mx-auto">
        <slot />
    </div>
</section>